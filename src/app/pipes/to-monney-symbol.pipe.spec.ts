import { ToMonneySymbolPipe } from './to-monney-symbol.pipe';

describe('ToMonneySymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ToMonneySymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
